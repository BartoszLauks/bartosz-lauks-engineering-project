{% extends 'Header.html.twig' %}

{% block title %}Form Test{% endblock %}

{% block body %}
    {{ dump() }}
    {# {{ form(form) }} #}
    {{ form_widget(form.language, { 'attr': {'class': 'select2'} }) }}
{% endblock %}