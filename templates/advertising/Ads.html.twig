{% block body %}
    {% if path == "default.png" %}
        <img src="{{ asset(path) }}" alt="">
    {% else %}
        <img src="{{ asset("/uploads/" ~ path) }}" style="max-height: 200px;max-width: 1000px" alt="">
    {% endif %}

{% endblock %}